# Document-Redactor
A Microsoft Word add-in that redacts sensitive PII (emails, phone numbers, SSNs) from documents, adds a confidentiality header, and enables Track Changes using the Word JavaScript API. Built with TypeScript and React.
